name: Helm Lint

on:
  workflow_call:

jobs:
  helm-lint:
    uses: kupecloud/github-workflows/.github/workflows/helm-lint.yaml@11b5503fe90f8de8fa6c7ceb32ef693f32524722 # v1.0.0
    with:
      chart-path: chart
      lint-args: --set tenant=example-tenant
