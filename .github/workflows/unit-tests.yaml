name: Unit Tests

on:
  pull_request:
    branches:
      - main
    paths:
      - "go.mod"
      - "**.go"
      - ".github/workflows/unit-tests.yaml"
      - "Makefile"
  workflow_call:

jobs:
  unit-tests:
    uses: kupecloud/github-workflows/.github/workflows/go-unit-tests.yaml@530d751e117f3a8451429d49e532a6df70b74432
