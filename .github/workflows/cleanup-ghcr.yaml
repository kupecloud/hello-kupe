name: Cleanup GHCR

on:
  schedule:
    - cron: "0 3 * * 1"
  workflow_dispatch:
    inputs:
      retention_days:
        description: Keep untagged versions updated within this many days.
        required: false
        type: number
        default: 7
      dry_run:
        description: Log candidates without deleting anything.
        required: false
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  cleanup:
    name: Cleanup GHCR untagged versions
    uses: kupecloud/github-workflows/.github/workflows/cleanup-ghcr.yaml@15393288c8f30a55feddaf965c70e9ccd2e916cc # v2.2.1
    permissions:
      packages: write
    with:
      retention_days: ${{ inputs.retention_days || 7 }}
      dry_run: ${{ inputs.dry_run || false }}
